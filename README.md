# 智能客服系统 - Customer Support Agent

基于 LangGraph 和 Vue3 的智能客服系统，使用 AI 自动分类、分析情感并处理客户查询。

## 项目结构

```
├── backend/                 # FastAPI 后端
│   └── main.py             # API 服务器
├── frontend/               # Vue3 前端
│   ├── src/
│   │   ├── App.vue         # 主应用组件
│   │   ├── main.js         # 入口文件
│   │   └── style.css       # 全局样式
│   ├── index.html
│   ├── package.json
│   └── vite.config.js
└── customer_support_agent_langgraph.py  # LangGraph Agent
```

## 功能特性

- 🤖 **智能分类**: 自动将查询分类为技术支持、账单查询或常规咨询
- 😊 **情感分析**: 分析用户情绪（积极、中性、消极）
- 🔀 **智能路由**: 根据分类和情感将查询路由到合适的处理器
- ⚠️ **自动升级**: 消极情绪的查询自动升级到人工客服
- 💬 **实时对话**: 现代化的聊天界面

## 快速开始

### 1. 安装后端依赖

```bash
# 在项目根目录
pip install fastapi uvicorn python-dotenv
```

### 2. 启动后端服务

```bash
# 在项目根目录
cd backend
python main.py
# 或者使用 uvicorn
uvicorn main:api --reload --port 8000
```

后端将运行在 http://localhost:8000

### 3. 安装前端依赖

```bash
cd frontend
npm install
```

### 4. 启动前端开发服务器

```bash
npm run dev
```

前端将运行在 http://localhost:5173

## API 端点

| 端点 | 方法 | 描述 |
|------|------|------|
| `/` | GET | 健康检查 |
| `/health` | GET | 服务状态 |
| `/api/chat` | POST | 处理客户查询 |
| `/api/categories` | GET | 获取可用分类 |

### 请求示例

```bash
curl -X POST http://localhost:8000/api/chat \
  -H "Content-Type: application/json" \
  -d '{"query": "我的网络连接不稳定"}'
```

### 响应示例

```json
{
  "query": "我的网络连接不稳定",
  "category": "Technical",
  "sentiment": "Neutral",
  "response": "非常抱歉您遇到了网络连接问题...",
  "status": "success"
}
```

## 环境变量

确保在项目根目录创建 `.env` 文件：

```env
OPENAI_API_KEY=your_api_key_here
OPENAI_API_BASE=your_api_base_url  # 如果使用自定义端点
```

## 技术栈

### 后端
- **FastAPI**: 高性能 Python Web 框架
- **LangGraph**: 构建 AI Agent 的状态图框架
- **LangChain**: LLM 应用开发框架
- **OpenAI/Qwen**: 大语言模型

### 前端
- **Vue 3**: 渐进式 JavaScript 框架
- **Vite**: 下一代前端构建工具
- **Axios**: HTTP 客户端

## 工作流程

```
用户查询 → 分类 → 情感分析 → 路由决策
                                    ↓
                    ┌───────────────┼───────────────┐
                    ↓               ↓               ↓
              技术支持处理     账单查询处理     常规咨询处理
                    ↓               ↓               ↓
                    └───────────────┼───────────────┘
                                    ↓
                               返回响应

如果情感为消极 → 升级到人工客服
```

## 开发说明

### 添加新的查询类别

1. 修改 `customer_support_agent_langgraph.py` 中的 `categorize` 函数
2. 添加新的处理函数 (如 `handle_new_category`)
3. 更新 `route_query` 函数的路由逻辑
4. 在 workflow 中添加新节点和边

### 自定义前端主题

修改 `frontend/src/style.css` 中的 CSS 变量：

```css
:root {
  --primary-color: #4f46e5;
  --primary-hover: #4338ca;
  /* ... */
}
```

## License

MIT
